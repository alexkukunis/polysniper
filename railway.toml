# Railway Configuration (Web Service)
# For Worker service, create a separate service in Railway dashboard

[build]
builder = "RAILPACK"

[deploy]
startCommand = "pnpm --filter @repo/web start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
